create or replace package body Yngvi.pak as 
procedure x(c varchar2(100)) is 
	x number;
	procedure g(d integer) is
	begin
		H:=Pakki.func(1);
	end;
begin
	x := 100 + 10;
	U := 'yngvi';
	B := H.K(1);
	Pakki.proc('ok');
	select c1 into v from dual;
end x;
end;
/

create or replace package body minni as 
procedure y(c varchar2(100)) is 
begin
	x := 20;
end y;
end;
/